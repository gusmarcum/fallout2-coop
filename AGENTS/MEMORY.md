# Memory Index

- [Code readability policy](code-readability-policy.md) — RE names stay until post-v1; docs/GLOSSARY.md (@d179bd5) = decoder ring + future rename map, agents append; rename pass is golden-safe bulk work AFTER v1 leash

- [Presenter object-ref model](presenter-object-ref-model.md) — REFERENCE primer: netId / NO_SAVE / serverLoopActive / OBJ_CREATE substrate the sequence presenter re-derives every session. Read BEFORE spelunking object.cc. Key finding: OBJ_CREATE is dead code for composite transients (created pre-SectionBegin → ship by netId → viewer drops them); only persistent-object refs replay.
- [Combat full record-channel](combat-full-record-channel.md) — in-combat record/replay channel BUILT + committed, combat presentation in good shape. Holds the load-bearing MENTAL MODEL (paced presentation over synchronous global engine state → convergent per-actor/per-seq scoping) + the remaining cosmetic tails (equip-while-gliding write-through fix, throw-retrieve held-disconnect, pickup-gesture decode-time adopt). Design=COMBAT_MOVE_RECORD_DESIGN.md (deleted).
- [Presentation viewer bugs](presentation-viewer-bugs.md) — BANKED open live-play bugs: BUG2 gib-corpse frame (polish); BUG3 can't-loot-gibbed; BUG5 NPC in-combat wield draw; BUG6 throw retrieve-cycle desync; BUG4 walk/run desync. Viewer-side = live-verify only.
- [Presentation record/replay build track](pres-record-build-track.md) — the record/replay channel = the presentation engine for every DISCRETE action. All Tier-1 families migrated to PRES_SEQ: gesture, door, explosion, weapon-draw (bespoke EVENT_DOOR_STATE/ACTION_ANIM/EXPLOSION_FX/WEAPON_TAKE_OUT handlers DELETED 2026-07-19, −461 lines). Remaining = bucket-C (nested re-entrant sections), receive-damage/hit-react, knockdown. Spec=docs/PRESENTATION_RECORD_REPLAY_SPEC.md.
- [F2 rewrite project](f2-rewrite-project.md) — branch rewrite/phase0 (STABLE substrate): docs of record + the 3-surface coverage model + the headless env/gate/reverse-eng reference. Live tracks = [[p5-server-plan]] + [[dialog-streaming-track]].
- [Model choice: Fable vs Opus](model-choice-fable-vs-opus.md) — Opus builds + adversarial Opus review (standing per-commit gate); Fable available but STAY ECONOMICAL ($20 Pro, shallow limits; old 3%-budget note was FALSE) — one tight Fable pass for genuinely design-class/cross-cutting/hard-to-reverse decisions, not mechanical ports or per-commit review
- [Memory/doc hygiene](memory-doc-hygiene.md) — mds & memory hold ONLY quirks / known issues / forward plan; NO changelog or "we resolved it"; completed → delete it (git is the changelog)
- [Working style: background agents & context](working-style-background-and-context.md) — proactively swarm ≤3 background recon/plan agents ahead of time; periodically advise a fresh session
- [Dialog headless plan](dialog-headless-plan.md) — the headless dialog engine (v1 shipped, golden-covered): durable STRUCTURE facts (dialog = a synchronous call stack nested in the interpreter), the data-model globals, probe verbs (dtalk/dsay/dend), and v1 scope boundaries. Reference substrate for [[dialog-streaming-track]].
- [Dialog streaming track](dialog-streaming-track.md) — DONE (2026-07-19). Stream dialog to the viewer. A0→A3 complete: engine linked into f2_server (A0), server dialogEnter handler (A1), block-and-pump barrier + dsay/dend routing (A2), viewer render + input + text rendering + hover highlight + barter→talk button (A3). Remaining polish: promote pump off F2_DIALOG_STREAM flag (needs adversarial review), F2_DIALOG_STREAM=1 required in viewer_live.sh for live-demo. Ladder in DIALOG_STREAMING_PLAN.md (deleted).
- [Barter streaming track](barter-streaming-track.md) — ►►ACTIVE. Wire the barter/trade modal for the viewer. Server-side engine already done (inventoryOpenTrade serverLoopActive drain); viewer-side = B0 (enter inventoryOpenTrade), B1 (table object netIds + inventory streaming), B2 (wire verbs), B3 (kBarter modal mask). Plan in BARTER_STREAMING_PLAN.md (deleted). Estimated 3-4 sessions.
- [Modal drivers plan](modal-drivers-plan.md) — ALL MODAL DRIVERS DONE (dialog/rest/endgame/worldmap/barter). Kept for the reusable driver PATTERN, the remaining low-pri UI-glue screens, + banked fidelity follow-ups (serverRun terminal-quit re-bless, freeplay-on-endgame, simClockReset statics).
- [Don't declare "not a bug" confidently](dont-declare-not-a-bug-confidently.md) — hedge all-clears (vanilla-faithful/fixed/SEPARATE); proving mechanism-A-isn't-it ≠ behavior-is-correct; verify vs a real oracle. User flagged this repeatedly 2026-07-17
- [Anim-decouple verification](anim-decouple-verification.md) — goldens pin determinism not correctness + no headless oracle; adversarial review is MANDATORY + richer dumps + invariant assertions
- [Holistic audit 2026-07-13](holistic-audit-2026-07-13.md) — Fable whole-rewrite audit: NO new bug. 3 banked UNPINNED test gaps: multi-victim knockback, approach-drop × script distance/LOS reads, DAM_KNOCKED_DOWN standup — each wants a fixture eventually.
- [P5 server plan](p5-server-plan.md) — ►► ACTIVE TRACK, RESUME PROTOCOL at top (read first). Dedicated server (f2_server) + SDL wire viewer; combat playable, the whole Temple solo path has run LIVE. Dialog streaming DONE (A0→A3). Replay unification DONE (all discrete actions → PRES_SEQ). Containers DONE (_obj_use_container decouple). Current pivot = [[barter-streaming-track]]. Open demo-critical: Temple traps (spatial-proc gap), intra-map elevation-follow, mid-run map-transition de-stub. Open bugs: 62s combat-state desync, skill-use gesture-replay flood, #2 run-anim slide, #10 glide-offset desync. Forward specs in bugs/: 005 lipsync audio, 006 in-combat medic skills, 007 character creation, 008 multi-player co-op. North star = docs/TEMPLE_DEMO_ROADMAP.md.
- [FOnline prior-art](fonline-prior-art.md) — read for IDEAS not code (licensing + clean-slate≠our singletons); PARKED, per-problem go/no-go; ROI = multi-map de-singleton / net-sync / client-prediction / modding sandbox
- IDEAS.md (repo root, now TRACKED steering doc @e357553) — feature vision + the architectural CONSTRAINTS that steer the rewrite: modding platform (server+client Lua, sandboxed/uncrashable), mod-defined custom-message channel on the wire's skip-unknown-T framing, save = engine-mechanism/mod-policy ctx, cars/hideouts/survival/crafting/factions, + the 2 hard walls to respect NOW (multi-map vs engine singletons; per-context var namespacing gvar_set(ctx,X,v)) + a FABLE vision-vs-impl audit checkpoint. FUTURE (banked, user): folder restructure server/core/client/{ui,hud,sfx…} + rename pass over annoying original fn names — heavy refactor, off critical path. Consult IDEAS.md for direction; see [[mp-actor-architecture-principle]].
- [P5 cut-list](p5-cut-list.md) — de-stub REFERENCE-CHAIN CUT-LIST. The RULE = restructure the reference out (no #ifdef/in-place guards). H1–H5 done (git). Remaining = H6 mapLoad chrome + the presenter/interface tail (⚠ interfaceGetCurrentHitMode = latent server crash). Anim family is NOT already-guarded. Run scripts/srv_sweep.sh FIRST to name real reached stubs.
- [Item PID glossary](item-pid-glossary.md) — docs/ITEM_GLOSSARY.md decodes 531 item PIDs; rule: PID==items.lst line index, name=pro_item.msg{PID*100}; DAT2 extractor recipe
- [id + cached-pointer antipattern](id-plus-cached-pointer-antipattern.md) — de-rot CLASS to hunt in the restructure pass: serialized id + stale cached runtime pointer, partial ad-hoc resolution (whoHitMe/whoHitMeCid exemplar, fixed 2026-07-18); 64-bit port reliably faults this latent UB — suspect it first on 64-bit crashes, do NOT go 32-bit to mask it
- [Frame-index render gotcha](frame-index-render-gotcha.md) — objectSetFid does NOT reset obj->frame; stale frame ≥ new art's frame count = renders NOTHING. Was the S5 corpse bug (fixed: objectSetFrame(0) in resolve). Latent bug class + usable hide-trick
- [Visual verification protocol](visual-verification-protocol.md) — NO screenshot-flood automation for presentation work; hand user a live-demo recipe + targeted questions; pkill -x the real binaries
- [Sweep before recon](sweep-before-recon-lesson.md) — run the thing on ALL inputs & let aborts name the work (beat 4 recon agents); ALWAYS ask "does the CLIENT do this too?" to separate pre-existing engine bugs from our regressions; harnesses must fail on build error
- [De-stub cadence rule](de-stub-cadence-rule.md) — BATCH hard, no per-change gate/review/milestone for trivial/benign/relocation changes (invisible to goldens); heavy review deferred to ONE massive pass; never run golden harnesses concurrently
- [Post-demo hardening](post-demo-hardening.md) — the 3 recurring "taxes" (leaf-enumeration / modal pump-and-bail dup / object-lifetime+A2) that make each viewer slice FEEL like rework; core IS convergent not band-aid; stay feature-first to Demo v1, pay down the tax the demo names
- [MP actor architecture principle](mp-actor-architecture-principle.md) — LOAD-BEARING invariant: a player is a first-class ACTOR; party/leash is a removable v1 behavior, NOT the player's identity. Never branch player logic on party-membership. Enumerates the party.cc assumptions we refuse to lock in + why engine singletons (not party) are the real roadmap wall. Locked: #1 actor model, #2 B-plus leashed. ►► UI-DRIVING COROLLARY (2026-07-16): gDude = per-client ROLE ("actor I control", per-client join binding); dude/critter duality is FREE (HP/AP on the object, per-actor by construction, NOT gvars → combat-presentation adds no gvar lock-in); guardrails now = key myTurn on netId not isPlayer, read local actor through gDude-role.
